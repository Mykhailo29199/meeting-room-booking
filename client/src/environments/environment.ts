/**
 * Production settings. `ng build` uses this file; development builds replace
 * it with `environment.development.ts` (see `fileReplacements` in angular.json).
 */
export const environment = {
  /**
   * Prefix for every API and hub URL, without a trailing slash. Empty means
   * the API is served from the same origin as the client.
   */
  apiBaseUrl: '',
};
