/**
 * Development settings (`ng serve`). The dev server proxies `/api` and `/hubs`
 * to the local API (proxy.conf.json), so requests stay on the client's origin.
 */
export const environment = {
  apiBaseUrl: '',
};
